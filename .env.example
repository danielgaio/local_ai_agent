# Example environment variables for local development
# Copy this file to .env and fill in values as needed.

# Choose model provider: 'ollama' (default) or 'openai'
# - 'ollama' expects a local Ollama daemon and models pulled (e.g., llama3.2:3b)
# - 'openai' requires an OpenAI API key and the langchain OpenAI integration
MODEL_PROVIDER=ollama

# When using OpenAI as provider, set your API key here (or export OPENAI_API_KEY directly)
OPENAI_API_KEY=

# If you want to force dummy embeddings (useful for testing without Ollama/OpenAI), set to 1
# In CI (GitHub Actions), dummy embeddings are automatically used unless overridden
USE_DUMMY_EMBEDDINGS=0

# Optional debug toggle to print internal debug logs
AIAGENT_DEBUG=0
